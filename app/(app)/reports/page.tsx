import Card from "@/components/Card";

export default function Reports() {
  return (
    <main>
      <h1 className="font-display text-2xl">Reports</h1>
      <Card className="mt-6 text-sm opacity-80">
        Weekly reports arrive Sunday evenings once you have a week of check-ins. Monthly report —
        with PDF — lands at day 30. (Phase 6)
      </Card>
    </main>
  );
}
